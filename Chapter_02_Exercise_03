# Exercise 3 : Write a program to prompt the user for hours and rate per hour to compute gross pay.

# 문제 3 : 총 비용을 계산하기 위한 시간과 시간 당 비용을 사용자에게 메시지를 보내는 프로그램을 작성하라.

prompt_hrs = 'Enter Hours : '
prompt_rate = 'Enter Rate : '

hrs = input(prompt_hrs)
rate = input(prompt_rate)

hrs = int(hrs)
rate = float(rate)

pay = hrs * rate
pay = str(pay)
print('Pay: ' + pay)
